import { Container } from 'react-bootstrap';

export default function DashboardPage() {
  return (
    <Container className="py-5 text-center">
      <p className="text-muted">Дашборд — реализуется в фазе 14.</p>
    </Container>
  );
}
